---
// pages/contato.astro - Página de Contato Institucional

import { Image } from 'astro:assets';
import MainLayout from '../layouts/mainLayout.astro';
import Heading from '../components/typograph/Heading.astro';
import Paragraph from '../components/typograph/Paragraph.astro';
import Button from '../components/buttons/mainButton.astro';
import DotsShape from '../components/shapes/dotsShape.astro';

// Dados da página
const pageData = {
  // SEO
  metaTitle: "Contato | Lúmino Clínica Integrada - Anápolis GO",
  metaDescription: "Entre em contato com a Lúmino Clínica. Atendimento personalizado em Anápolis. WhatsApp, telefone, formulário online. Agende sua consulta.",
  
  // Hero
  hero: {
    titulo: "Entre em contato",
    subtitulo: "FALE CONOSCO",
    descricao: "Estamos prontos para atender você com todo carinho e profissionalismo. Escolha o canal de sua preferência e agende sua consulta.",
    heroImage: "/images/contato-hero.jpg"
  },
  
  // Informações de contato
  contato: {
    whatsapp: "62 98326-5797",
    whatsappLink: "5562983265797",
    telefone: "62 3321-1234",
    email: "contato@luminoclinica.com.br",
    endereco: {
      rua: "Av. Brasil Sul, 845",
      bairro: "Jundiaí",
      cidade: "Anápolis - GO",
      cep: "75113-570"
    },
    googleMapsLink: "https://maps.google.com/maps?q=Lumino+Clinica+Anapolis",
    instagramLink: "https://instagram.com/luminoclinica",
    facebookLink: "https://facebook.com/luminoclinica"
  },
  
  // Horários
  horarios: [
    { dia: "Segunda a Sexta", horario: "08:00 às 18:00" },
    { dia: "Sábado", horario: "08:00 às 12:00" },
    { dia: "Domingo", horario: "Fechado" }
  ],
  
  // FAQ Contato
  faqContato: [
    {
      pergunta: "Vocês atendem por convênio?",
      resposta: "Sim! Atendemos diversos convênios odontológicos. Entre em contato para verificar se atendemos o seu plano específico."
    },
    {
      pergunta: "Preciso agendar consulta com antecedência?",
      resposta: "Recomendamos o agendamento com antecedência para garantir o melhor horário para você. Em casos de urgência, entre em contato que faremos o possível para atendê-lo."
    },
    {
      pergunta: "Vocês têm estacionamento?",
      resposta: "Sim, oferecemos estacionamento gratuito para nossos pacientes com segurança e comodidade."
    },
    {
      pergunta: "Atendem crianças?",
      resposta: "Sim! Temos especialistas em odontopediatria e um ambiente preparado para receber os pequenos com todo carinho."
    }
  ]
};

// Imports de imagens mock
import heroImage from '../assets/images/bg.jpg';
import clinicaImage from '../assets/images/clinica-recepcao.jpg';
import equipeImage from '../assets/images/equipe-atendimento.jpg';

const whatsappMessage = encodeURIComponent(`Olá! Vim pelo site e gostaria de mais informações.`);
const whatsappLink = `https://wa.me/${pageData.contato.whatsappLink}?text=${whatsappMessage}`;
---

<MainLayout title={pageData.metaTitle} description={pageData.metaDescription}>
  <!-- Hero Section -->
  <section class="h-[60vh] bg-white flex items-center overflow-hidden relative">
    <div class="hero-bg">
      <Image
        src={heroImage}
        alt="Contato Lúmino Clínica"
        class="hero-bg-image"
        layout="full-width"
        loading="eager"
        width={1920}
        height={1080}
      />
    </div>
    <div class="counter-particles"></div>
    
    <div class="mx-auto max-w-[1600px] lg:pl-40 w-full z-10">
      <div class="lg:w-[50%] px-8 lg:px-0 z-3">
        <span class="text-primary-gold font-bold animate-subtitle font-serif text-sm tracking-wider">
          {pageData.hero.subtitulo}
        </span>
        <h1 class="text-5xl md:text-6xl 2xl:text-7xl font-bold font-cormorant animate-title mt-4 leading-tight">
          {pageData.hero.titulo} <span class="text-primary-gold">conosco</span>
        </h1>
        <p class="text-lg text-gray-600 animate-description mt-6 leading-relaxed">
          {pageData.hero.descricao}
        </p>
      </div>
      <DotsShape />
    </div>
  </section>

  <!-- Seção de Contatos Rápidos -->
  <section class="section bg-primary-dark text-white">
    <div class="shape shape-3"></div>
    <div class="container">
      <div class="grid md:grid-cols-3 gap-8">
        <a href={whatsappLink} target="_blank" class="group cursor-pointer">
          <div class="text-center reveal-effect transform transition-all duration-500 group-hover:-translate-y-3">
            <div class="w-24 h-24 mx-auto mb-6 bg-white/10 rounded-full flex items-center justify-center text-4xl group-hover:bg-primary-gold transition-all duration-500">
              📱
            </div>
            <h3 class="text-2xl font-bold mb-2 text-primary-gold">WhatsApp</h3>
            <p class="text-xl text-white/90">{pageData.contato.whatsapp}</p>
            <p class="text-sm text-white/70 mt-2">Resposta rápida</p>
          </div>
        </a>
        
        <a href={`tel:${pageData.contato.telefone.replace(/\s/g, '')}`} class="group cursor-pointer">
          <div class="text-center reveal-effect transform transition-all duration-500 group-hover:-translate-y-3">
            <div class="w-24 h-24 mx-auto mb-6 bg-white/10 rounded-full flex items-center justify-center text-4xl group-hover:bg-primary-gold transition-all duration-500">
              📞
            </div>
            <h3 class="text-2xl font-bold mb-2 text-primary-gold">Telefone</h3>
            <p class="text-xl text-white/90">{pageData.contato.telefone}</p>
            <p class="text-sm text-white/70 mt-2">Atendimento comercial</p>
          </div>
        </a>
        
        <a href={`mailto:${pageData.contato.email}`} class="group cursor-pointer">
          <div class="text-center reveal-effect transform transition-all duration-500 group-hover:-translate-y-3">
            <div class="w-24 h-24 mx-auto mb-6 bg-white/10 rounded-full flex items-center justify-center text-4xl group-hover:bg-primary-gold transition-all duration-500">
              ✉️
            </div>
            <h3 class="text-2xl font-bold mb-2 text-primary-gold">E-mail</h3>
            <p class="text-lg text-white/90">{pageData.contato.email}</p>
            <p class="text-sm text-white/70 mt-2">Para documentos e orçamentos</p>
          </div>
        </a>
      </div>
    </div>
  </section>

  <!-- Formulário e Informações -->
  <section class="section bg-white">
    <div class="shape shape-1"></div>
    <div class="container">
      <div class="grid lg:grid-cols-2 gap-16">
        <!-- Formulário -->
        <div class="reveal-effect">
          <h2 class="section-title mb-8">
            Envie sua <span class="accent">mensagem</span>
          </h2>
          <form id="contact-form" class="space-y-6">
            <div class="grid md:grid-cols-2 gap-6">
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">Nome completo</label>
                <input 
                  type="text" 
                  name="nome" 
                  required
                  class="w-full px-6 py-4 rounded-2xl border border-gray-200 focus:outline-none focus:border-primary-gold transition-all duration-300"
                  placeholder="Seu nome"
                />
              </div>
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">Telefone</label>
                <input 
                  type="tel" 
                  name="telefone" 
                  required
                  class="w-full px-6 py-4 rounded-2xl border border-gray-200 focus:outline-none focus:border-primary-gold transition-all duration-300"
                  placeholder="(00) 00000-0000"
                />
              </div>
            </div>
            
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2">E-mail</label>
              <input 
                type="email" 
                name="email" 
                required
                class="w-full px-6 py-4 rounded-2xl border border-gray-200 focus:outline-none focus:border-primary-gold transition-all duration-300"
                placeholder="seu@email.com"
              />
            </div>
            
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2">Assunto</label>
              <select 
                name="assunto" 
                required
                class="w-full px-6 py-4 rounded-2xl border border-gray-200 focus:outline-none focus:border-primary-gold transition-all duration-300"
              >
                <option value="">Selecione um assunto</option>
                <option value="agendamento">Agendamento de consulta</option>
                <option value="orcamento">Solicitar orçamento</option>
                <option value="duvida">Tirar dúvidas</option>
                <option value="emergencia">Urgência/Emergência</option>
                <option value="outro">Outro assunto</option>
              </select>
            </div>
            
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2">Mensagem</label>
              <textarea 
                name="mensagem" 
                rows="4"
                required
                class="w-full px-6 py-4 rounded-2xl border border-gray-200 focus:outline-none focus:border-primary-gold transition-all duration-300 resize-none"
                placeholder="Como podemos ajudar?"
              ></textarea>
            </div>
            
            <button 
              type="submit"
              class="w-full bg-primary-gold text-white px-8 py-4 rounded-full font-bold text-lg hover:bg-gold-dark transition-all duration-300 transform hover:scale-105 shadow-xl"
            >
              Enviar mensagem
            </button>
          </form>
        </div>
        
        <!-- Informações -->
        <div class="reveal-effect">
          <h2 class="section-title mb-8">
            Informações de <span class="accent">atendimento</span>
          </h2>
          
          <!-- Endereço -->
          <div class="mb-8">
            <div class="flex items-start gap-4 mb-6">
              <div class="w-14 h-14 bg-light-bg rounded-2xl flex items-center justify-center text-2xl flex-shrink-0">
                📍
              </div>
              <div>
                <h3 class="text-xl font-bold text-primary-dark mb-2">Endereço</h3>
                <p class="text-gray-600">
                  {pageData.contato.endereco.rua}<br/>
                  {pageData.contato.endereco.bairro}<br/>
                  {pageData.contato.endereco.cidade}<br/>
                  CEP: {pageData.contato.endereco.cep}
                </p>
                <a 
                  href={pageData.contato.googleMapsLink}
                  target="_blank"
                  class="text-primary-gold hover:text-gold-dark transition-colors mt-2 inline-block"
                >
                  Ver no Google Maps →
                </a>
              </div>
            </div>
          </div>
          
          <!-- Horários -->
          <div class="mb-8">
            <div class="flex items-start gap-4 mb-6">
              <div class="w-14 h-14 bg-light-bg rounded-2xl flex items-center justify-center text-2xl flex-shrink-0">
                🕐
              </div>
              <div class="w-full">
                <h3 class="text-xl font-bold text-primary-dark mb-4">Horário de Atendimento</h3>
                <div class="space-y-3">
                  {pageData.horarios.map((horario) => (
                    <div class="flex justify-between items-center py-2 border-b border-gray-100">
                      <span class="font-medium text-gray-700">{horario.dia}</span>
                      <span class="text-primary-gold font-medium">{horario.horario}</span>
                    </div>
                  ))}
                </div>
              </div>
            </div>
          </div>
          
          <!-- Redes Sociais -->
          <div>
            <div class="flex items-start gap-4">
              <div class="w-14 h-14 bg-light-bg rounded-2xl flex items-center justify-center text-2xl flex-shrink-0">
                🌐
              </div>
              <div>
                <h3 class="text-xl font-bold text-primary-dark mb-4">Redes Sociais</h3>
                <div class="flex gap-4">
                  <a 
                    href={pageData.contato.instagramLink}
                    target="_blank"
                    class="w-12 h-12 bg-primary-dark text-white rounded-xl flex items-center justify-center hover:bg-primary-gold transition-all duration-300 transform hover:scale-110"
                  >
                    <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                      <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zM5.838 12a6.162 6.162 0 1112.324 0 6.162 6.162 0 01-12.324 0zM12 16a4 4 0 110-8 4 4 0 010 8zm4.965-10.405a1.44 1.44 0 112.881.001 1.44 1.44 0 01-2.881-.001z"/>
                    </svg>
                  </a>
                  <a 
                    href={pageData.contato.facebookLink}
                    target="_blank"
                    class="w-12 h-12 bg-primary-dark text-white rounded-xl flex items-center justify-center hover:bg-primary-gold transition-all duration-300 transform hover:scale-110"
                  >
                    <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                      <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                    </svg>
                  </a>
                  <a 
                    href={whatsappLink}
                    target="_blank"
                    class="w-12 h-12 bg-green-500 text-white rounded-xl flex items-center justify-center hover:bg-green-600 transition-all duration-300 transform hover:scale-110"
                  >
                    <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                      <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z"/>
                    </svg>
                  </a>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Mapa -->
  <section class="h-[500px] relative">
    <iframe 
      src={pageData.contato.googleMapsLink}
      width="100%" 
      height="100%" 
      style="border:0;" 
      allowfullscreen="" 
      loading="lazy" 
      referrerpolicy="no-referrer-when-downgrade"
      class="w-full h-full"
    ></iframe>
    
    <!-- Overlay com informações -->
    <div class="absolute top-8 left-8 bg-white rounded-2xl shadow-2xl p-6 max-w-sm">
      <h3 class="font-bold text-xl text-primary-dark mb-2">Lúmino Clínica Integrada</h3>
      <p class="text-gray-600 text-sm mb-4">
        {pageData.contato.endereco.rua}, {pageData.contato.endereco.bairro}
      </p>
      <a 
        href={pageData.contato.googleMapsLink}
        target="_blank"
        class="text-primary-gold hover:text-gold-dark transition-colors text-sm font-medium"
      >
        Obter direções →
      </a>
    </div>
  </section>

  <!-- FAQ -->
  <section class="section bg-light-bg">
    <div class="shape shape-2"></div>
    <div class="container">
      <div class="max-w-4xl mx-auto">
        <div class="text-center mb-16">
          <h2 class="section-title section-title-center">
            Perguntas <span class="accent">frequentes</span>
          </h2>
          <p class="section-subtitle section-subtitle-center">
            Tire suas dúvidas sobre nosso atendimento
          </p>
        </div>
        
        <div class="space-y-4">
          {pageData.faqContato.map((item, index) => (
            <div class="bg-white rounded-2xl overflow-hidden reveal-effect shadow-sm hover:shadow-lg transition-shadow duration-300">
              <button 
                class="w-full text-left p-6 font-bold text-primary-dark hover:bg-light-bg transition-colors duration-300 flex items-center justify-between faq-toggle"
                data-target={`faq-${index}`}
              >
                <span class="flex items-center">
                  <span class="mr-4 text-2xl text-primary-gold">❓</span>
                  {item.pergunta}
                </span>
                <svg class="w-5 h-5 transition-transform duration-300 faq-icon text-primary-gold" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                </svg>
              </button>
              <div id={`faq-${index}`} class="hidden p-6 pt-0 text-gray-700 leading-relaxed faq-content">
                {item.resposta}
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Final -->
  <section class="section contact-cta">
    <div class="contact-cta-shape contact-cta-shape-1"></div>
    <div class="contact-cta-shape contact-cta-shape-2"></div>
    
    <div class="contact-cta-container">
      <h2 class="contact-cta-title">
        Prefere falar pelo <span class="accent">WhatsApp?</span>
      </h2>
      <p class="contact-cta-text">
        Resposta rápida e atendimento personalizado. Clique no botão abaixo e fale conosco agora mesmo!
      </p>
      
      <div class="contact-cta-buttons">
        <Button 
          title="📱 Chamar no WhatsApp" 
          href={whatsappLink}
          variant="gold"
        />
        <Button 
          title="📞 Ligar agora" 
          href={`tel:${pageData.contato.telefone.replace(/\s/g, '')}`}
          variant="outline-white"
        />
      </div>
    </div>
  </section>
</MainLayout>

<script>
  // Inicialização
  document.addEventListener('DOMContentLoaded', function() {
    // FAQ Toggle
    const faqToggles = document.querySelectorAll('.faq-toggle');
    faqToggles.forEach(toggle => {
      toggle.addEventListener('click', function() {
        const targetId = this.getAttribute('data-target');
        const faqContent = document.getElementById(targetId);
        const icon = this.querySelector('.faq-icon');
        
        if (faqContent.classList.contains('hidden')) {
          // Fecha todos os outros
          document.querySelectorAll('.faq-content').forEach(content => {
            content.classList.add('hidden');
          });
          document.querySelectorAll('.faq-icon').forEach(faqIcon => {
            faqIcon.style.transform = 'rotate(0deg)';
          });
          
          // Abre o atual
          faqContent.classList.remove('hidden');
          icon.style.transform = 'rotate(180deg)';
        } else {
          faqContent.classList.add('hidden');
          icon.style.transform = 'rotate(0deg)';
        }
      });
    });

    // Form Handler
    const contactForm = document.getElementById('contact-form');
    if (contactForm) {
      contactForm.addEventListener('submit', function(e) {
        e.preventDefault();
        
        const formData = new FormData(contactForm);
        const nome = formData.get('nome');
        const telefone = formData.get('telefone');
        const email = formData.get('email');
        const assunto = formData.get('assunto');
        const mensagem = formData.get('mensagem');
        
        // Validação
        if (!nome || !telefone || !email || !assunto || !mensagem) {
          alert('Por favor, preencha todos os campos.');
          return;
        }
        
        // Mensagem para WhatsApp
        const whatsappMessage = `*Novo contato via site*
        
👤 *Nome:* ${nome}
📱 *Telefone:* ${telefone}
📧 *E-mail:* ${email}
📋 *Assunto:* ${assunto}
💬 *Mensagem:* ${mensagem}`;

        const whatsappUrl = `https://wa.me/5562983265797?text=${encodeURIComponent(whatsappMessage)}`;
        
        // Feedback visual
        const button = contactForm.querySelector('button[type="submit"]');
        const originalText = button.innerHTML;
        
        button.innerHTML = '✅ Enviando...';
        button.disabled = true;
        
        // Simula envio (aqui você integraria com seu backend)
        setTimeout(() => {
          button.innerHTML = '✅ Mensagem enviada!';
          
          // Abre WhatsApp
          window.open(whatsappUrl, '_blank');
          
          // Reset form
          setTimeout(() => {
            contactForm.reset();
            button.innerHTML = originalText;
            button.disabled = false;
          }, 3000);
        }, 1500);
      });
    }

    // Reveal animations
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -100px 0px'
    };

    const observer = new IntersectionObserver(function(entries) {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('active');
        }
      });
    }, observerOptions);

    document.querySelectorAll('.reveal-effect').forEach(el => {
      observer.observe(el);
    });

    // Smooth scroll for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          target.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      });
    });
  });
</script>

<style>
  /* Estilos específicos para a página de contato */
  .contact-info-card {
    background: white;
    border-radius: 20px;
    padding: 2rem;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
    transition: all 0.3s ease;
  }
  
  .contact-info-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
  }
  
  /* Animação para o botão de envio */
  @keyframes pulse {
    0% {
      box-shadow: 0 0 0 0 rgba(201, 168, 116, 0.7);
    }
    70% {
      box-shadow: 0 0 0 10px rgba(201, 168, 116, 0);
    }
    100% {
      box-shadow: 0 0 0 0 rgba(201, 168, 116, 0);
    }
  }
  
  button[type="submit"]:not(:disabled):hover {
    animation: pulse 2s infinite;
  }
  
  /* Estilo para o mapa */
  iframe {
    filter: grayscale(20%);
    transition: filter 0.3s ease;
  }
  
  iframe:hover {
    filter: grayscale(0%);
  }
</style>